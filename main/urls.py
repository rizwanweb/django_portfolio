from django.urls import path

from . import views

urlpatterns = [
    # path('', views.IndexView_as_view(), name='home'),
    # path('contact/', views.ContactView_as_view(), name='contact'),
    # path('portfolio/', views.PortfolioView_as_view(), name='portfolio'),
    # path('portfolio/<slug:slug>', views.PortfolioDetailView_as_view(),
    #      name='portfolio_detail'),
    # path('blog/', views.BlogView_as_view(), name='blog'),
    # path('blog/<slug:slug>', views.BlogDetailView_as_view(), name='blog_detail'),
]
